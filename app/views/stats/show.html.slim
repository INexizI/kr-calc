=> link_to 'Back', stats_path
'&bull;
=< link_to 'Edit', edit_stat_path(@stat)

hr

.hero
  .p-title
    = @stat.name
  .p-title
    = @stat.value
  .p-title
    - if !@stat.role.blank?
      = @stat.role.name
