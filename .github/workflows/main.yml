name: CI

on:
  push:
    branches: master
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: Setup reviewdog
        uses: reviewdog/action-setup@v1.0.3
